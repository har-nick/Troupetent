import java.time.ZonedDateTime;
import kotlin.collections.List;
import uk.co.harnick.bandkit.data.model.Track;

CREATE TABLE Playlist (
    ownerId INTEGER NOT NULL,
    name TEXT NOT NULL,
    dateTimeCreated TEXT AS ZonedDateTime NOT NULL,
    trackList TEXT AS List<Track>,
    FOREIGN KEY(ownerId) REFERENCES Account(id) ON DELETE CASCADE
);

load: SELECT * FROM Playlist WHERE ownerId = ?;
save: REPLACE INTO Playlist VALUES (?, ?, ?, ?);
delete: DELETE FROM Playlist WHERE ownerId = ?;
